import { NavList } from '../NavList/NavList';
import { XiaomiLogo } from '../XiaomiLogo/XiaomiLogo';

export const Header = () => {
  return (
    <header>
      <NavList />
      <XiaomiLogo />
    </header>
  );
};
